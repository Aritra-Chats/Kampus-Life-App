package com.example.feature_auth

/**
 * AuthSession tests require the Android runtime (Firebase SDK).
 * Run these as instrumented tests on a real device/emulator instead.
 *
 * Unit-testable logic (parseKiitEmail, inferRoleFromEmail, etc.)
 * is covered in AuthUtilsTest.
 */
